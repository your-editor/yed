# lib_dir needs to be visible to dlopen().
# /usr/lib and friends are the best bet, but any path can be used if it is
# added to LD_LIBRARY_PATH.
if [ -d "/usr/lib64" ]; then
    lib_dir="/usr/lib64"
else
    lib_dir="/usr/lib"
fi

plug_dir="${lib_dir}/yed/plugins"
bin_dir="/usr/bin"
inc_dir="/usr/include"
share_dir="/usr/share"

# This will force us to get the home directory of the
# user even when `sudo` is being used.
eval HOME_DIR=$(echo ~$(logname))

user_dir="${HOME_DIR}/.yed"
