# install.options

prefix=/usr # typical system install
# prefix=~/.local # typical local user install

lib_dir=${prefix}/lib
plug_dir=${lib_dir}/yed/plugins
bin_dir=${prefix}/bin
inc_dir=${prefix}/include
share_dir=${prefix}/share
